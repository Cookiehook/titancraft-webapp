[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "*"  # Web framework
psycopg2-binary = "*"  # Postgres DB Driver
django-markdownify = "*"  # Markdown for location descriptions
boto3 = "*"  # AWS SDK. Used to retrieve RDS password
requests = "*"  # HTTP requests to Discord API
gunicorn = "*"  # Production webserver
supervisor = "*"  # Process manager for nginx and gunicorn

[requires]
python_version = "3.9"
